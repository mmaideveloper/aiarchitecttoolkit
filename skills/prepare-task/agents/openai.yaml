interface:
  display_name: "Prepare Task"
  short_description: "Prepare traceable implementation-ready tasks"
  default_prompt: "Use $prepare-task to create an implementation-ready task from the linked idea and architecture artifacts without inventing unresolved decisions."
