interface:
  display_name: "Generate Business Decision Record"
  short_description: "Record traceable business decisions"
  default_prompt: "Use $generate-bdr to compare options and create a source-backed Business Decision Record."
